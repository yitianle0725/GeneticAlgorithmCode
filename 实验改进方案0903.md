我：
我是想问你我的 实验代码目前的 设计如何



----------------
gpt:
明白了。你问的不是“这三个问题该怎么做”，而是：**你目前这套对比实验代码本身设计得怎么样。**

我的判断是：

> 作为“算法原型与初步对比平台”，设计是比较完整的；但作为用来支撑论文结论的正式实验，严谨性还不够，目前更像探索性实验。

我会给它三个不同维度的评价：

| 维度                 | 评价 |
| -------------------- | ---: |
| 代码工程结构         | 7/10 |
| 四算法初步性能对比   | 6/10 |
| 支撑三个科学问题     | 2/10 |
| 直接用于论文正式实验 | 3/10 |

## 做得比较好的地方

### 1. 对比算法选择合理

你选择了：

* NSGA-II：基于支配和拥挤度；
* NSGA-III：基于支配和参考方向；
* MOEA/D：基于分解；
* IEMOEC：你的独立进化与极值组合算法。

这个组合是有意义的，因为它不只是“随便选四个算法”，而是覆盖了三种不同搜索机制。

特别是：

* 与 NSGA-II 比较，可以考察拥挤度机制；
* 与 NSGA-III 比较，可以考察 many-objective 下的参考方向机制；
* 与 MOEA/D 比较，可以考察子问题分工与独立种群分工的差别。

因此，**baseline 的总体方向是对的**。

### 2. many-objective 维度设置比较完整

代码支持：

$$
M=3,5,8,10,15
$$

这比只测试 2、3 个目标好得多，能够初步观察目标数量增加后的性能变化。

其中：

* \(M=3\)：普通多目标；
* \(M=5\)：many-objective 的过渡；
* \(M=8,10,15\)：可以明显观察 Pareto dominance 选择压力下降的问题。

### 3. 测试问题具有一定互补性

你主要比较了：

* DTLZ1：线性 PF、多局部最优；
* DTLZ2：规则凹面 PF；
* DTLZ3：多峰、收敛困难；
* DTLZ4：分布偏置；
* C-DTLZ2：凸前沿。

这几类问题分别考察收敛、多峰搜索、分布偏置和不同 PF 形状，作为第一阶段测试集是合理的。

### 4. 指标不算单一

你记录了：

* IGD；
* GD；
* HV；
* SP；
* ONVG。

至少已经区分了：

* 收敛性；
* 分布均匀性；
* 非支配解数量；
* 综合解集质量。

代码也把算法、孤岛、指标、测试函数、结果汇总分成不同文件，后续增加消融实验不会特别困难。

---

# 目前最主要的问题

## 1. 最大的问题：四个算法的计算预算不公平

你现在基本上是按照“代数”设置算法，而不是统一函数评价次数 FE。

但 IEMOEC 的一轮外循环包含：

* \(2M\) 个孤岛；
* 每岛 20 个个体；
* 每岛运行 30 或 70 代；
* 每岛选择 3 个精英；
* 所有精英两两交叉；
* 最后还有 PF 扩展。

所以 IEMOEC 的一个“外循环”远远大于 NSGA-II 的一代。

按当前默认配置估算：

| \(M\) | 基线最大 FE | IEMOEC 约 FE |  倍数 |
| ----: | ----------: | -----------: | ----: |
|     3 |      36,892 |      138,184 | 3.75× |
|     5 |     127,412 |      237,624 | 1.86× |
|     8 |      90,120 |      397,040 | 4.41× |
|    10 |     220,220 |      510,840 | 2.32× |
|    15 |     180,120 |      819,840 | 4.55× |

因此，如果 IEMOEC 的 IGD 更好，审稿人第一反应会是：

> 是不是因为 IEMOEC 评估了解更多解？

正式实验必须给所有算法设置相同的：

$$
MaxFEs
$$

然后横轴也要使用：

$$
Function\ Evaluations
$$

不能用 generation。

---

## 2. 当前 IEMOEC 实际上是一个混合算法

目前代码中的 IEMOEC 同时使用了：

* 独立孤岛；
* 极端岛和折中岛；
* Tchebycheff 聚合；
* Pareto 非支配排序；
* Crowding Distance；
* NSGA-III 参考向量小生境；
* 最终 PF 扩展。

所以当前算法更准确的定位是：

> **Independent-evolution hybrid MOEA**

而不是一个干净的：

> dominance + independent evolution + extrema combination

这不是说混合不好，而是它会导致创新贡献不清楚。

如果结果好，别人不知道究竟是哪一个机制起作用。

---

## 3. 文档中的算法定义和代码不一致

你的附件里说：

* 初始起源种群约为普通 GA 的 \(1/5\)；
* 不使用拥挤度；
* 依靠独立进化保持多样性；
* 算法复杂度更低。

但当前代码中：

### 起源种群不是 \(1/5\)

```python
self.n_origin = self.config.N_ORIGIN or self.config.POPSIZE
```

即默认等于 NSGA-III 的完整种群规模。

### 实际使用了 Crowding Distance

岛内选择多个精英时，第二个精英就是拥挤度最大的个体。

### 外层使用 NSGA-III 小生境

因此多样性不完全来自独立进化。

### 实际计算量更大

特别是所有精英两两组合，复杂度会随目标数明显增加。

所以需要先决定：

> 以文档中的原始 MOEC 为准修改代码，还是以当前混合 IEMOEC 为准重新描述算法？

目前两者不能混着写。

---

## 4. 极值组合设计过于“暴力”

目前有 \(2M\) 个岛，每岛输出 3 个精英，因此共有：

$$
K=6M
$$

个精英。

全部两两重组产生：

$$
2\binom{6M}{2}=6M(6M-1)
$$

个后代。

当 \(M=15\) 时：

$$
K=90
$$

每轮产生：

$$
90\times89=8010
$$

个重组后代。

这会带来三个问题：

* 计算量太大；
* 大量亲本之间可能没有必要组合；
* 算法很难支撑“低复杂度、实时性好”的说法。

更合适的方式可能是：

* 每个极端岛只与若干互补岛组合；
* 每个精英随机选择固定数量的跨岛亲本；
* 按目标互补度选择组合；
* 每轮设置固定重组预算。

例如每个精英只选择 \(r=2\) 或 \(r=3\) 个跨岛亲本，复杂度就能从：

$$
O(M^2)
$$

接近降到：

$$
O(M)
$$

---

## 5. `parallel_evolution` 目前没有真正并行

函数名叫：

```python
island_parallel_evolution()
```

但代码实际是：

```python
for island in self.islands:
    for _ in range(gens):
        island.evolve_generation(...)
```

也就是一个岛接一个岛串行运行。

它在算法逻辑上是“相互独立”，但在执行层面不是并行计算。

因此目前可以说：

> 具有天然并行潜力。

但不能根据现有实现直接说：

> 已经通过并行提高了运行效率。

如果论文强调实时性，需要使用多进程或分布式实现，并单独报告 wall-clock time 和 speedup。

---

## 6. 目前实际上只有一次独立运行

虽然命令行提供了：

```bash
--seed
```

但是 `run_single()` 里固定执行：

```python
random.seed(123)
np.random.seed(123)
```

传入的 `--seed` 没有真正向下传递。

因此现在每个配置基本只有一次固定种子运行。

正式实验一般至少需要：

$$
20\text{或}30
$$

次独立运行，然后报告：

$$
mean\pm std
$$

并进行：

* Wilcoxon rank-sum；
* Friedman test；
* Holm post-hoc correction。

单次运行只能叫“初步结果”，不能证明算法稳定优于 baseline。

---

## 7. 当前比较脚本过于依赖“最终 IGD”

`compare_iemoec.py` 虽然读取了五个指标，但主要展示最终 IGD 和获胜次数。

“25 个问题赢了多少次”比较直观，但论文证据不足，因为：

* 不包含随机波动；
* 没有显著性检验；
* 不同问题的 IGD 数值尺度不同；
* 一次微小领先也会记为一次胜利；
* 没有统一 FE。

更合理的结果表应该是：

```text
mean ± std
+
显著性符号 + / = / -
+
平均排名
```

---

## 8. 当前 HV 的计算方式不适合跨算法比较

你现在对每个算法的当前解集分别进行 min-max 归一化：

$$
F'=\frac{F-F_{\min}}{F_{\max}-F_{\min}}
$$

然后计算 HV。

问题是：每个算法使用自己的 \(F_{\min}\) 和 \(F_{\max}\)。

假设一个算法的解集整体距离真实 PF 很远，只要解集内部形状相似，归一化之后仍可能得到较高 HV。

因此正式比较应使用所有算法共享的：

* ideal point；
* nadir point；
* HV reference point；
* 归一化范围。

否则当前 HV 只能用于观察单次运行内部变化，不能可靠地横向比较四个算法。

---

## 9. 早停机制对算法不完全公平

基线算法使用 HV 停滞进行早停；但高维情况下 HV 不是每代重新计算，而是隔几代沿用旧值。

这样未计算 HV 的代数中：

$$
\Delta HV=0
$$

可能会被误认为“算法没有改善”。

而 IEMOEC 的外循环上限是 20，早停耐心值是 30，实际上一般不会触发相同的早停。

所以四个算法不但 FE 不相同，停止条件也不相同。

正式比较建议直接取消这种早停，全部运行到统一 MaxFEs。

---

# 我对当前设计的最终定位

你的代码并不是“设计得很差”。相反，它已经完成了一个挺不错的实验原型：

```text
测试函数
  ↓
四种算法
  ↓
多种目标维度
  ↓
统一指标输出
  ↓
自动汇总比较
```

真正的问题是：

> 它目前适合发现现象，但不适合证明原因。

它可以初步告诉你：

> IEMOEC 在哪些 DTLZ 问题上可能表现不错。

但还不能告诉你：

* 是不是独立进化保持了多样性；
* 是不是极值组合增强了全局探索；
* 是不是独立种群缓解了 dominance selection pressure；
* 还是仅仅因为更多 FE、Tchebycheff、Crowding Distance 和 NSGA-III 小生境共同起作用。

因此下一阶段最合理的工作顺序是：

1. 先统一 MaxFEs 和停止条件；
2. 修复随机种子并运行 30 次；
3. 统一 HV 归一化；
4. 让文档算法描述与代码一致；
5. 增加独立进化、极值组合、Crowding Distance 的消融版本；
6. 最后再讨论三个科学问题是否成立。

一句话评价：

> **这是一套基础不错的 IEMOEC 探索性实验代码，但当前变量控制不足、计算预算不公平、实现与理论描述不一致；经过一次实验框架重构后，才适合用于论文级验证。**



---------------------------
我：
那接下来应该如何改进实验代码呢？
我有几个问题：1.我随机种子跑一次就需要非常久的时间（4个算法好几个 目标好几轮迭代，要两三个h） ，跑30次会不会1天都跑不完？
2.HV每代都计算，耗时更久
3.多维情况下，好像没法作图？
4.只跑了DTLZ问题，其他经典测试函数还没加
5.代码算法不知道是否正确



------------------------
gpt:
接下来不应该立刻增加更多测试函数或直接跑 30 次。正确顺序是：

> **先验证算法正确性与实验公平性，再优化运行效率，然后才扩展测试集和重复次数。**

否则程序跑得越久，只是在更大规模地生成可能不可靠的结果。

## 推荐的改进顺序

| 阶段 | 目标                                   | 优先级 |
| ---- | -------------------------------------- | -----: |
| 1    | 修复种子、FE、HV、停止条件等实验问题   |   最高 |
| 2    | 用 pymoo 验证 NSGA-II/III、MOEA/D 实现 |   最高 |
| 3    | 减少单次实验耗时，支持并行与断点续跑   |     高 |
| 4    | 增加多随机种子和统计检验               |     高 |
| 5    | 增加 WFG、MaF、LSMOP 等测试集          |     中 |
| 6    | 增加 IEMOEC 消融实验                   |     高 |
| 7    | 统一生成论文表格和图片                 |   最后 |

---

# 1. 跑30次会不会一天都跑不完？

会。按照你现在的速度：

$$
1个seed=2\sim3小时
$$

串行跑 30 次就是：

$$
60\sim90小时
$$

大约需要 2.5～4 天，而且当前 IEMOEC 的函数评价次数比其他算法高，运行时间还不完全公平。

但正式实验不需要在开发阶段每次都跑 30 次。

## 建议使用三级实验模式

### 第一级：正确性测试

只运行：

* DTLZ2；
* \(M=3\)；
* 1 个 seed；
* 较小 MaxFEs；
* 四个算法。

目标不是比较性能，而是检查：

* 能否正常运行；
* 种群规模是否正确；
* 指标是否正常；
* 是否出现 NaN；
* FE 是否正确统计。

预计几分钟。

### 第二级：预实验

运行：

* DTLZ1、DTLZ2、DTLZ3、DTLZ4；
* \(M=3,5,10\)；
* 5 个 seed；
* 4 个算法。

共：

$$
4\times3\times5\times4=240
$$

个独立任务。

这一阶段用来发现趋势、调整 MaxFEs，不用于最终论文结论。

### 第三级：正式实验

算法、参数和测试集全部确定以后，再运行：

* 20 或 30 个 seed；
* 完整问题集；
* 完整目标维度；
* 固定 MaxFEs。

不要一边调参数一边跑 30 次，否则前面的结果很可能全部作废。

## 多核并行运行

每个“算法 + 问题 + 目标数 + seed”都是独立任务，非常适合多进程并行。

例如你的电脑有 8 核，可以同时运行 6～8 个任务。理论上 60～90 小时能压缩到约 10～15 小时。

但建议只使用一层并行：

```text
并行不同实验任务
```

暂时不要同时：

```text
外层实验并行 + IEMOEC孤岛并行
```

否则容易导致 CPU 过度竞争、内存占用过高。

## 当前必须先修复随机种子

你的命令行虽然支持：

```bash
--seed
```

但 `run_single()` 仍固定使用 123，参数没有真正传递下去。

应改成：

```text
run.py --seed
    ↓
run_batch(seed)
    ↓
run_single(seed)
    ↓
Algorithm(seed)
```

并为每个问题使用相同的种子集合：

```python
SEEDS = [1, 2, 3, ..., 30]
```

例如 DTLZ2-M5 上，四个算法都使用同一组种子，后面就可以做配对统计检验。

---

# 2. HV 每代计算太慢怎么办？

不应该每代计算 HV，也不应该继续用 HV 做早停条件。

## 建议把“运行指标”和“最终指标”分开

### 运行过程中

只记录便宜的内容：

* FE；
* 当前第一前沿大小；
* IGD 或 IGD+；
* 运行时间；
* 第一前沿占比。

每达到固定 FE 检查点再记录一次，例如：

$$
5\%,10\%,15\%,\ldots,100\%
$$

整个运行过程只记录 20 个点。

### 实验结束后

再计算：

* 最终 HV；
* 最终 IGD+；
* Spacing；
* diversity；
* 统计结果。

这样 HV 每次实验只需要计算一次。

如果需要 HV 收敛曲线，可以只在：

```text
10%、20%、30%、……、100% MaxFEs
```

这 10 个检查点计算。

## 不同目标数使用不同 HV 方法

|      目标数 | 推荐方法                       |
| ----------: | ------------------------------ |
|  \(M\leq5\) | 精确 HV                        |
|     \(M>5\) | Monte Carlo HV 或近似 HV       |
| \(M\geq10\) | IGD+、R2 为主要指标，HV 为补充 |

高维 HV 本来就很昂贵，不需要强行每代精确计算。

## 当前 HV 还需要修复归一化

现在每个算法用自己的当前种群进行 min-max 归一化，这会让跨算法 HV 不可比。

应该针对同一个测试问题，统一指定：

```python
ideal_point
nadir_point
hv_reference_point
```

所有算法、所有 seed 都使用相同范围。

Monte Carlo HV 也应预先生成一套固定采样点，供所有算法共同使用，避免每次重复生成和计算。

## 停止条件改为统一 MaxFEs

正式实验建议取消基于 HV 的提前终止，统一使用：

```python
while evaluator.n_eval < max_fes:
    ...
```

这样四个算法得到完全相同的评价预算。

---

# 3. 多维情况下是不是没法作图？

可以作图，但不能直接画普通二维 Pareto 散点图。

## 不同维度使用不同图

|       目标维度 | 推荐图形                       |
| -------------: | ------------------------------ |
|              2 | 二维 Pareto 散点图             |
|              3 | 三维 Pareto 散点图             |
|        4及以上 | 平行坐标图                     |
|       任意维度 | IGD+/HV 随 FE 变化曲线         |
|       任意维度 | 多次运行箱线图                 |
| 多算法总体比较 | 平均排名图、胜/平/负统计       |
| many-objective | 参考方向占用率或网格覆盖热力图 |

## 高维最有价值的图不是 Pareto Front 图

正式论文中，高维实验通常更应该展示：

### 1. 收敛曲线

横轴：

$$
Function\ Evaluations
$$

纵轴：

$$
Median\ IGD^+
$$

不是画 30 条种子曲线，而是画：

* 中位数；
* 25%～75%区间；
* 或均值和标准差阴影。

### 2. 箱线图

展示 20/30 次最终 IGD+ 分布，可以看出：

* 中位数；
* 稳定性；
* 异常值；
* 算法之间是否明显分离。

### 3. 平行坐标图

每一条线是一组非支配解，每一根纵轴是一个目标。

但是必须使用所有算法共享的归一化范围，否则图形不能比较。

### 4. 第一前沿比例曲线

这是第三个科学问题特别需要的图：

$$
R_{ND}=\frac{|F_1|}{N}
$$

横轴是目标数 \(M\)，纵轴是第一非支配层占比。

如果 NSGA-II 在 \(M=15\) 时接近 1，而独立进化版本仍然保持有效搜索进展，就能观察 dominance selection pressure 的变化。

PCA、t-SNE 可以作为辅助展示，但不能作为算法性能证据，因为降维会扭曲原始目标空间。

---

# 4. 除了 DTLZ，还应该增加什么测试函数？

先不要一下子加几十个。建议分三层。

## 第一层：保留 DTLZ1–4

它们适合做基础正确性验证：

* DTLZ1：多峰、线性 PF；
* DTLZ2：规则 PF；
* DTLZ3：强多峰；
* DTLZ4：偏置分布。

你当前的 C-DTLZ2 也可以保留，但要先验证实现和真实 PF 是否正确。

## 第二层：加入 WFG1–9

WFG 比 DTLZ 更复杂，可以测试：

* 非可分离变量；
* 欺骗性；
* 多峰；
* 偏置；
* 不规则 PF；
* 退化 PF；
* 混合形状。

对于证明“极值组合是否提高全局探索”，WFG 和 DTLZ3 会比单纯 DTLZ2 更有价值。

推荐第一批先加：

* WFG1；
* WFG2；
* WFG4；
* WFG9。

不用一开始就全部跑完。

## 第三层：根据论文问题选择

如果研究 many-objective：

* MaF1–MaF15；
* 或选择其中具有不同特性的代表问题。

如果研究 large-scale multiobjective：

* LSMOP1–LSMOP9；
* 决策变量设置为 \(D=100,500,1000\)。

你当前最大只有约 34 个决策变量，所以现在只能声称研究 many-objective，不能声称研究 large-scale。

如果以后研究约束问题，再加入：

* C-DTLZ；
* DC-DTLZ；
* DAS-CMOP。

目前没有必要把约束问题也立即加进来。

建议最终形成：

```text
基础：DTLZ1–4
复杂前沿：WFG1/2/4/9
many-objective：部分 MaF
large-scale：部分 LSMOP
```

---

# 5. 怎么确认代码中的算法是正确的？

这是现在最应该优先解决的问题。

你目前的 NSGA-II、NSGA-III 和 MOEA/D 都是自己实现的。即使程序可以运行，也不能说明实现与原论文完全一致。

## 最可靠的方法：用 pymoo 作为参考实现

建议把实验系统改为：

```text
NSGA-II ─┐
NSGA-III ├── 直接使用 pymoo
MOEA/D  ─┘

IEMOEC ───── 保留自定义实现
```

这样有三个好处：

* baseline 的正确性不需要你独自承担；
* DTLZ、WFG、MaF 等问题可以直接复用；
* 指标、终止条件和 FE 统计更容易统一。

如果你希望保留自己的 baseline 实现，也至少要与 pymoo 做差分验证。

例如在相同条件下运行：

```text
你的 NSGA-II
pymoo NSGA-II
```

比较：

* 种群大小；
* FE 数量；
* 最终 IGD+；
* 最终 HV；
* Pareto Front 形状；
* 是否出现明显异常。

结果不需要完全相同，因为随机过程不同，但整体数量级和趋势不能相差很大。

## 应增加的单元测试

### 非支配排序

人为构造几个点：

```text
A = (1, 1)
B = (2, 2)
C = (1, 3)
D = (3, 1)
```

应当满足：

* A 支配 B、C、D；
* A 位于第一层；
* 其他点位于后续层。

### Crowding Distance

检查：

* 每个目标的边界点距离为无穷；
* 中间点距离非负；
* 目标完全相同时不会除零。

### SBX 和多项式变异

检查：

* 子代始终在变量上下界内；
* 没有 NaN；
* 父代不会被意外原地修改；
* 设置固定 seed 后结果可复现。

### DTLZ 函数

人工构造满足：

$$
g(x)=0
$$

的决策变量，检查输出是否满足真实 PF 方程。

例如 DTLZ2 应满足：

$$
\sum_{i=1}^{M}f_i^2=1
$$

### FE 统计

所有目标函数计算都必须经过同一个 evaluator：

```python
evaluator.evaluate(individual)
evaluator.n_eval += 1
```

不能让某些初始化、PF 扩展或重组评价漏记。

### IEMOEC 结构测试

检查：

* 岛之间没有共享同一个 Individual 对象；
* 一个岛的变异不会修改另一个岛的个体；
* 每轮起源者数量稳定；
* 每岛种群大小稳定；
* 极值组合后代数量符合预算；
* 关闭重组后确实没有跨岛基因交流。

---

# IEMOEC 本身还需要调整

## 1. 限制极值组合预算

不要让全部精英两两组合。

可以改成：

```python
offspring_budget = remaining_fes
```

每个精英只和若干互补精英组合，生成数量固定的子代。

这样既公平，也能避免 \(M=15\) 时每轮生成 8010 个重组后代。

## 2. 明确是否保留 Crowding Distance

建议建立两个版本：

```text
IEMOEC-CD
IEMOEC-NoCD
```

不要让论文说“不使用拥挤度”，代码里却在精英选择时使用拥挤度。

## 3. 明确是否保留 NSGA-III 外层选择

也可以建立：

```text
IEMOEC-Pareto
IEMOEC-NSGA3
```

前者只使用非支配排序和简单截断，后者使用参考向量。

这样才能知道多样性来自独立进化，还是来自 NSGA-III。

## 4. 删除最终 PF 扩展，或者计入 FE

当前实验结束后又通过变异和 NSGA-III 填充 PF，这会提高最终指标。

如果保留，必须：

* 计入 MaxFEs；
* 所有算法提供相同的后处理预算。

更建议正式实验中删除这种专属于 IEMOEC 的额外扩展。

---

# 推荐的新实验架构

每一个独立任务都定义为：

```python
ExperimentCase(
    algorithm="IEMOEC",
    problem="DTLZ3",
    n_obj=10,
    seed=7,
    max_fes=100000,
)
```

统一输出：

```text
results/
  DTLZ3/
    M10/
      IEMOEC/
        seed_007/
          config.json
          history.csv
          final_population.csv
          metrics.json
```

其中 `history.csv` 只在固定 FE 检查点记录：

```text
FE, IGD+, HV, ND_ratio, runtime
```

实验系统还应支持：

* 多进程并行；
* 已完成任务自动跳过；
* 失败任务单独重跑；
* 不在运行过程中画图；
* 所有实验完成后统一画图；
* 每次保存完整配置；
* 汇总 mean、median、std；
* Wilcoxon/Friedman 统计检验。

## 最实际的下一步

第一轮修改先只做下面六件事：

1. 修复 `--seed` 传递；
2. 加入统一 FE 计数器和 MaxFEs；
3. 删除 HV 早停；
4. HV 改成最终计算或固定检查点计算；
5. 删除/计入 IEMOEC 的额外 PF 扩展；
6. 使用 pymoo 对照验证三个 baseline。

做完以后，先用：

```text
DTLZ2
M=3
seeds=[1,2,3]
较小 MaxFEs
```

验证整个实验系统。确认正确后，再增加 WFG 和完整的多种子实验。

最重要的一句话是：

> **现在的首要任务不是“把实验跑得更多”，而是让每一次实验都正确、公平、可复现；运行速度可以通过稀疏计算 HV、统一 FE 和任务级并行解决。**
